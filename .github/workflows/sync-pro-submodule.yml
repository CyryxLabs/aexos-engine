name: Retired Pro Submodule Sync

on:
  workflow_dispatch:

permissions: {}

jobs:
  sync:
    name: Refuse obsolete submodule synchronization
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired distribution path
        run: |
          echo "::error::Submodule synchronization is retired under ADR-AEX-011. Core CI validates the private signed-artifact boundary; no gitlink is synchronized."
          exit 1
