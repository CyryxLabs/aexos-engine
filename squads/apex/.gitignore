

# Environment & Secrets (AEXOS)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AEXOS)
node_modules/
node_modules

# Build & Logs (AEXOS)
dist/
build/
*.log
logs/

# IDE & OS (AEXOS)
.DS_Store
Thumbs.db
.idea/
*.swp

# AEXOS Local (AEXOS)
.aexos-core/local/
.claude/settings.local.json
.aexos/install-log.txt

# Apex Runtime (caches, pipeline state, handoffs)
.aexos/apex-context/
.aexos/apex-pipeline/
.aexos/handoffs/
